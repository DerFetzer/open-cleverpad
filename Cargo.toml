# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g. crates.io) dependencies
#
# If you believe there's an error in this file please file an
# issue against the rust-lang/cargo repository. If you're
# editing this file be aware that the upstream Cargo.toml
# will likely look very different (and much more reasonable)

cargo-features = ["profile-overrides"] # +

[package]
name = "open-cleverpad"
version = "0.1.0"
authors = ["DerFetzer <kontakt@der-fetzer.de>"]
edition = "2018"

# this lets you use `cargo fix`!
[[bin]]
name = "open-cleverpad"
test = false
bench = false

[profile.release]
lto = true
codegen-units = 1
debug = true

[profile.dev.overrides."*"]
codegen-units = 1
opt-level = "z"

[dependencies.stm32f1xx-hal]
version = "0.3.0"
features = ["stm32f103", "rt"]

[dependencies.cortex-m]
version = "0.6.0"
# "inline-asm" optimizes away AsmDelay in release mode
#features = ["inline-asm", "const-fn"]
features = ["const-fn"]

[dependencies.cortex-m-rt]
version = "0.6.10"
features = ["device"]

[dependencies.cortex-m-rtfm]
version = "0.4.3"
features = ["timer-queue", "nightly"]
# To be able to use "nightly" feature you might have to add "maybe_uninit_extra" feature gate to this dependencies
# source code.

[dependencies.cortex-m-semihosting]
version = "0.3.3"

[dependencies.panic-semihosting]
version = "0.5.2"

[dependencies]
asm-delay = "0.7.0"
bitrate = "0.1.1"
panic-halt = "0.2.0"
usb-device = "0.2.2"
stm32-usbd = { version = "0.3", features = ["stm32f103xx"] }
heapless = "0.5.0"